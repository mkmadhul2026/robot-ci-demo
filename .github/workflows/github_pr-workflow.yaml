name: PR check Info

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  all-tests:

    runs-on: [self-hosted]

    steps:

        - name: Checkout repository
          uses: actions/checkout@v4

        - name: Check PR info
          run: |
            python robotfw_learning/robot-ci-demo/robot-ci-demo/scripts/check_pr_info.py    

            