name: PR check Info

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  pr-check-info:

    runs-on: [self-hosted]

    steps:

        - name: Checkout repository
          uses: actions/checkout@v4

        - name: Check PR info
          run: |
            $env:PYTHONPATH = "$env:GITHUB_WORKSPACE"
            python scripts/check_pr_info.py    

            